/**
 * OCR Components
 *
 * Components for displaying OCR extraction results (text and images).
 * All components follow design tokens and use space-y-4 for sections.
 */

export { TextViewer } from './TextViewer';
export type { TextViewerProps } from './TextViewer';
